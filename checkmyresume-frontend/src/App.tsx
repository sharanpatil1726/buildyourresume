import './App.css'

export default function App() {
  return (
    <main className="app-shell">
      <section className="card">
        <h1>CheckMyResume</h1>
        <p>Your resume parser API is deployed and the frontend build is configured.</p>
      </section>
    </main>
  )
}
